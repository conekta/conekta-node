var assert = require("assert"),
    request = require('request');

describe('conekta', function() {
    describe('Charge', function() {
        it('create', function() {
            assert(true, true);
        });
    });
});
